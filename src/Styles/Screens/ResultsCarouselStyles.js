import {
  StyleSheet,
} from 'react-native';

export default StyleSheet.create({
  container: {
    position: 'absolute',
    alignItems: 'center',

  },

  title: {
    fontSize: 40,
    paddingTop: 40
  },

  percentage_text: {
    position: 'absolute',
    left: 122,
    top: 220,
    fontSize: 30,
    fontWeight: 'bold'
  }

});
