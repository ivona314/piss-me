Nakon instaliranja novog libraryja pomocu npm-a, potrebno je:

`npm run build:ios` 

to izgenerira file **main.jsbundle**

Taj file je potrebno dodati u Xcode-u u `Build Phases > Copy Bundle Resources`